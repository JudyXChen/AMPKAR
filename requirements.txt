# Requirements for AMPK project
# Note: some packages (e.g. jax/jaxlib) have platform-specific wheels.
# Install jax and jaxlib following official instructions: https://github.com/google/jax

# Core scientific computing
numpy>=2.0  # pymc>=5.21.0 supports numpy 2.x
openpyxl
pandas>=2.2
scipy>=1.15
sympy
xarray

# Plotting and visualization
matplotlib>=3.10
seaborn
met_brewer

# JAX ecosystem (pinned for RecursiveCheckpointAdjoint + VJP compatibility)
jax==0.4.38
jaxlib==0.4.38
equinox==0.11.12
diffrax==0.6.2
lineax==0.0.7
optimistix==0.0.10

# Probabilistic programming / Bayesian inference
pymc>=5.28.0
pytensor>=2.37.0
numpyro
arviz>=0.19.0
nutpie>=0.16.5
pymc-extras>=0.6.0
preliz

# Sensitivity analysis
SALib

# Jupyter notebook widgets
ipywidgets
